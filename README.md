```shell
docker build -t <image_name> .

docker tag <image_name>:<vesrion> <username>/<repo>:<version>

docker build -t <image>:<new_tag>

docker push <image>:<new_tag>

docker push <username>/<docker_repo>:<version>
```

````
tag == <part>.<exercise>
tag == 1.1 
````